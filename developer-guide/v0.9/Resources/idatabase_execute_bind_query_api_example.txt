		DatabaseDescriptor databaseDescriptor = Resources.getInstance().getDatabaseDescriptorBasedOnName(database-descriptor-name);
		IDatabase database = null;
		
		try {
			database = Database.createDatabase(databaseDescriptor);
		} catch(DatabaseException databaseException) {
			//Log It.
		}


		try {
			database.executeBindQuery(Query, Column Values);
		} catch(DatabaseException databaseException) {
			// Log It.
		}
